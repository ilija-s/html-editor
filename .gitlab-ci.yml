image: ubuntu:latest

stages:
  - before_script
  - build
  - test


before_script:
  - apt-get update -y
  - apt-get install -y build-essential
  - apt-get install -y qt64base
build:
  stage: build
  script:
    - qmake ./HTML_editor.pro
    - make -j$(nproc)

test:
  stage: test
  script:
    - cd test
    - for dir in */; do
        if [[ $dir == *Test/ ]]; then
          cd $dir;
          qmake;
          make;
          ./$(basename $dir)Test;
          cd ..;
        fi
      done
